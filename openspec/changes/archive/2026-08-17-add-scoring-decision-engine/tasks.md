## 1. Establish the ECO-12 baseline and traceability map

- [x] 1.1 Re-read the Change artifacts, `openspec/specs/unit-economics-engine/spec.md`, `product_research/evidence.py`, `product_research/unit_economics.py`, `references/scoring-policy.md`, `references/gates.md`, `docs/product-research-skill-spec.md`, and `SKILL.md`; record a requirement/scenario-to-test map before editing production code.
- [x] 1.2 Run the existing Phase 3 and Unit Economics suites plus the full `test_*.py` discovery as a pre-change baseline; preserve any unrelated work and stop if an existing failure affects ECO-12 evidence.
- [x] 1.3 Confirm the implementation allowlist is limited to `product_research/scoring_decision.py`, `tests/test_scoring_decision.py`, `SKILL.md`, `references/scoring-policy.md`, `references/gates.md`, and `docs/product-research-skill-spec.md`; do not modify upstream modules, package exports, unrelated tests, configuration, or data.

## 2. Write focused RED contracts first

- [x] 2.1 Add failing tests for the exact eight-dimension vocabulary/order, immutable value objects and aggregates, strict Decimal-only `[0, 100]` scores, `None` as unresolved, required Evidence IDs for concrete scores, duplicate-ID rejection, and unchanged Confidence.
- [x] 2.2 Add failing tests for the frozen `20/15/20/15/10/8/7/5` Base Weights, a complete explicit zero-adjustment vector, exact `-5` / `+5` acceptance, out-of-range and unsupported adjustment rejection, missing/extra adjustment failure, exact final `100` total, and proof that the executor never creates its own adjustment set.
- [x] 2.3 Add failing weighted-score tests for exact Base Weight and adjusted calculations, fixed dimension/formula order, non-terminating Decimal stability under the 34-digit `ROUND_HALF_EVEN` contract, repeated evaluation, different ambient Decimal contexts, arithmetic failure conversion, and no mutation of the global context.
- [x] 2.4 Add failing core-threshold tests for all four frozen thresholds, equality-as-PASS, any positive Decimal shortfall as FAIL, unresolved as UNRESOLVED, fixed result/failed-dimension order, and a high aggregate that cannot hide a core failure.
- [x] 2.5 Add failing upstream-contract tests for `RiskGateState` `CLEAR` / `REVIEW_REQUIRED` / `FATAL`, missing or malformed Risk as review-required, direct retention and consumption of an existing `UnitEconomicsResult`, each `EconomicsOutcome`, malformed economics as unresolved, and proof that economics arithmetic or Risk research is never rerun.
- [x] 2.6 Add failing decision-policy tests for Decimal-only GO threshold validation in `[0, 100]`, equality-as-pass, below-threshold behavior, and missing/invalid threshold producing structured non-GO results without a default.
- [x] 2.7 Add failing precedence tests for fatal Risk and economics `UNVIABLE` producing `NO-GO`, Risk review producing `RISK REVIEW`, simultaneous hard failure plus Risk review preserving `NO-GO`, and aggregate/core/weight/scoring conditions never overriding either higher-priority gate.
- [x] 2.8 Add failing conditional-path tests for economics `BELOW_TARGET` / `UNRESOLVED`, missing or invalid required scores, invalid weights, failed or unresolved core thresholds, absent aggregate, missing/invalid GO policy, and aggregate below threshold; prove only the complete all-pass case returns `GO`.
- [x] 2.9 Add failing result/diagnostic tests for immutability, the exact closed reason vocabulary and priority, reason deduplication, retention of simultaneous higher- and lower-priority conditions, ordered failed/unresolved dimensions, lexical Evidence-ID union/deduplication, per-score traceability, and no aggregate Confidence invention.
- [x] 2.10 Add a failing purity and ownership audit proving the new capability imports no clock, network, random, LLM, persistence, Evidence Policy, or Evidence Assessment dependency and exposes no research, score generation, Dynamic Weight selection, Risk analysis, Unit Economics calculation, Red Team, revision, report, serialization, orchestration, or autonomous-decision behavior.

## 3. Implement the minimal deterministic capability

- [x] 3.1 Add only `product_research/scoring_decision.py` with the closed `Dimension`, `CoreOutcome`, `RiskGateState`, `DecisionLabel`, and `DecisionReason` vocabularies plus the frozen input, policy, detail, and result values required by the RED tests.
- [x] 3.2 Implement strict `DimensionScore` and `DimensionScores` validation, exact fixed-field dimension ordering, unresolved-score handling, concrete-score Evidence-ID requirement, lexical per-score IDs, duplicate rejection, and defensive malformed-aggregate diagnostics without rereading Evidence or changing Confidence.
- [x] 3.3 Implement the complete explicit `WeightAdjustments` contract, frozen Base Weights, Decimal-only inclusive adjustment bounds, exact final-total validation, ordered `DimensionWeight` results, and no implicit zero vector or automatic adjustment behavior.
- [x] 3.4 Implement a fresh module-local 34-digit `ROUND_HALF_EVEN` Decimal context and ordered weighted aggregate calculation with no quantization, partial aggregation, weight renormalization, or ambient-context dependence; convert ordinary calculation failures into structured diagnostics.
- [x] 3.5 Implement independent ordered `CoreThresholdResult` evaluation for the four frozen thresholds, including actual values, equality-inclusive comparison, failed-core dimensions, and unresolved-core behavior.
- [x] 3.6 Implement direct validation and consumption of the explicit Risk state and existing `UnitEconomicsResult` / `EconomicsOutcome`, retaining the valid upstream economics result and mapping only decision-facing diagnostics without invoking upstream evaluators.
- [x] 3.7 Implement `DecisionPolicy` with only the optional explicit GO threshold and implement `evaluate_scoring_decision(...)` as the single fail-closed public entry point that accumulates safe diagnostics before applying the frozen `NO-GO` → `RISK REVIEW` → `CONDITIONAL GO` → `GO` precedence.
- [x] 3.8 Make the focused suite GREEN, then simplify only code introduced by this Change; remove any new unused imports/helpers and confirm every production line traces to a delta-spec requirement.

## 4. Route living documentation narrowly

- [x] 4.1 Update `references/scoring-policy.md` to route normalized score/weight execution, aggregate calculation, core evaluation, explicit GO policy, and final analytical labels to `product_research/scoring_decision.py`; retain upstream responsibility for qualitative score generation and Dynamic Weight justification.
- [x] 4.2 Update `references/gates.md` to describe the minimal decision-facing Risk state and direct `UnitEconomicsResult` consumption while keeping Risk research and Unit Economics calculation owned by their existing stages.
- [x] 4.3 Update `SKILL.md` routing and capability-gap text so deterministic scoring/decision execution is implemented but research acquisition, qualitative score generation, Risk scanning, Red Team, persistence, reporting, and full workflow orchestration remain unavailable.
- [x] 4.4 Narrowly update `docs/product-research-skill-spec.md` where its scoring, core threshold, gate precedence, and final-label sections would otherwise contradict the executor; do not claim Phase 6/7 analysis, Red Team, report, or end-to-end evaluation is implemented.
- [x] 4.5 Review the four documents together to ensure they do not duplicate Decimal internals or invent an aggregate GO/severe-failure threshold and that they preserve analytical-label-not-commercial-decision language.

## 5. Verify focused, regression, and scope gates

- [x] 5.1 Run `/usr/bin/python3 -m unittest discover -s tests -p 'test_scoring_decision.py' -v`, repeat the ambient-context, Evidence-order, simultaneous-precedence, malformed-boundary, and missing-policy cases, and trace every delta-spec scenario to fresh focused test evidence.
- [x] 5.2 Run `/usr/bin/python3 -m unittest discover -s tests -p 'test_evidence_data_model.py' -v`, `/usr/bin/python3 -m unittest discover -s tests -p 'test_evidence_policy.py' -v`, `/usr/bin/python3 -m unittest discover -s tests -p 'test_evidence_assessment.py' -v`, and `/usr/bin/python3 -m unittest discover -s tests -p 'test_unit_economics.py' -v` separately.
- [x] 5.3 Run `/usr/bin/python3 -m unittest discover -s tests -p 'test_*.py' -v` and every other repository-wide automated validation present at Apply time; leave the full suite green.
- [x] 5.4 Run `openspec validate add-scoring-decision-engine --strict` and `openspec validate --all --strict`, then inspect the final diff for float coercion, Unknown-to-zero behavior, partial aggregation, implicit adjustments/thresholds, score-based `NO-GO`, gate overrides, Evidence/economics duplication, LLM/research behavior, mutable configuration, upstream edits, package-export expansion, or unrelated changes.
- [x] 5.5 Obtain an independent acceptance review tracing proposal, design, every requirement/scenario, tasks, implementation, documentation, and fresh command output; resolve all in-scope findings and leave archive, commit, and push unperformed pending separate authorization.
