## 1. Establish RED acceptance and domain contracts

- [x] 1.1 Re-read `proposal.md`, `design.md`, the delta spec, current Phase 3 modules/specs/tests, `references/gates.md`, `references/scoring-policy.md`, and `SKILL.md` at Apply time; confirm `product_research/unit_economics.py` remains the smallest supported boundary and record any changed repository fact before production edits.
- [x] 1.2 Add Unit Economics acceptance scenarios to `tests/scenarios.md` for fixed explicit inputs, missing-versus-zero, Unknown propagation, currency mismatch, explicit policy, independent gates, combined outcomes, deterministic ordering, Decimal-context independence, purity, and the ECO-12 boundary.
- [x] 1.3 Create `tests/test_unit_economics.py` with a deliberate missing-module assertion and failing construction tests for all closed vocabularies and immutable input, aggregate, policy, derived-value, gate-result, and final-result types; run `/usr/bin/python3 -m unittest discover -s tests -p 'test_unit_economics.py' -v` and record the expected RED result.
- [x] 1.4 Add failing tests that accept finite `Decimal`, reject float/string/non-finite amounts, enforce Unknown/amount consistency, validate three-uppercase-letter currency, reject zero/negative Selling Price and negative costs, allow explicit zero costs, reject duplicate per-input Evidence IDs, and normalize reordered IDs; rerun and record RED.
- [x] 1.5 Add failing tests that require all eight named fields with no defaults, calculate exact Contribution Profit and fractional Contribution Margin for all-Observed and mixed Observed/Estimated inputs, preserve negative Contribution Profit, and never turn omission into zero; rerun and record RED.

## 2. Establish RED propagation, policy, and determinism contracts

- [x] 2.1 Add a table-driven failing test that makes each required input independently `Unknown`; assert ordered `unresolved_inputs`, Unknown/Low derived values, `UNKNOWN_REQUIRED_INPUT`, unresolved gates, no partial calculation, and no Unknown-to-zero conversion.
- [x] 2.2 Add failing Confidence tests for all-High, any-Medium, any-Low, strict weakest-input propagation, and unresolved-to-Low behavior; prove no average, weight, Tier rule, assessment call, or source-text inference affects derived Confidence.
- [x] 2.3 Add failing traceability tests for lexical per-input normalization, duplicate rejection within one input, cross-input Evidence-ID union/deduplication, reordered equivalent IDs, repeated evaluation, and stable derived/result ID ordering without modifying any Evidence value.
- [x] 2.4 Add failing currency tests for same-currency calculation, malformed codes, mismatch across every concrete field position, explicit Unknown currency handling, and structured `CURRENCY_MISMATCH` with no FX lookup or conversion.
- [x] 2.5 Add failing policy tests for finite Decimal thresholds, rejected float/string/non-finite thresholds, independently missing Minimum and Dynamic values, no default constants, no hidden range, consistent equality, inconsistent target-below-minimum policy, and no category or heuristic target generation.
- [x] 2.6 Add failing gate tests for Minimum and Dynamic `PASS` / `FAIL` / `UNRESOLVED`, equality-as-PASS, actual and threshold preservation, independently evaluated subresults, and invalid policy producing unresolved gates while preserving valid calculated economics.
- [x] 2.7 Add failing combined-outcome tests for `UNVIABLE`, `BELOW_TARGET`, `MEETS_TARGET`, every specified `UNRESOLVED` path, Minimum failure with missing Dynamic Target, and Minimum pass with missing Dynamic Target.
- [x] 2.8 Add failing diagnostics tests for the exact closed reason vocabulary, fixed reason priority/deduplication, formula-ordered unresolved inputs, malformed aggregate/input handling, invalid amount/Selling Price, calculation exception, missing policy, invalid policy, and safely preserved partial diagnostics.
- [x] 2.9 Add failing Decimal replay tests for exact formula order, a non-terminating margin pinned to 34 significant digits with `ROUND_HALF_EVEN`, different ambient precision/rounding/traps, repeated identical evaluation, overflow/error conversion, and proof that the global Decimal context is unchanged.
- [x] 2.10 Add a failing purity/ownership test or source audit proving the capability imports no clock, network, random, LLM, third-party, Evidence Policy, or Evidence Assessment dependency and exposes no scoring, Risk, final-decision, persistence, serialization, acquisition, FX, or report behavior.

## 3. Implement the minimal deterministic capability

- [x] 3.1 Add only `product_research/unit_economics.py` with reused `EvidenceId`, `Status`, and `Confidence`; implement the closed Unit Economics vocabularies and frozen input, aggregate, policy, derived-value, gate-result, and final-result values needed by the RED contracts without modifying Phase 3 modules or package exports.
- [x] 3.2 Implement strict EconomicInput and aggregate validation, Decimal-only amount/status rules, formula-position sign rules, structural currency validation, per-input Evidence-ID duplicate rejection/normalization, and the fixed eight-field aggregate; make the construction/input contracts pass without coercion or defaults.
- [x] 3.3 Implement a fresh module-local 34-digit `ROUND_HALF_EVEN` Decimal context with explicit exponent bounds and traps, the ordered Contribution Profit subtraction, fractional Contribution Margin division, negative-profit preservation, and exception-to-`CALCULATION_ERROR`; make calculation and ambient-context tests pass.
- [x] 3.4 Implement full Unknown propagation, formula-ordered unresolved inputs, weakest-input Confidence propagation, lexical Evidence-ID union, derived `Calculated` or `Unknown` states, and safely resolved currency behavior; make propagation and traceability tests pass.
- [x] 3.5 Implement explicit optional policy validation, threshold consistency, equality-inclusive Minimum/Dynamic comparison, independent gate results, and the closed economics-outcome precedence without defaults, heuristics, scores, or decision labels; make policy/gate/outcome tests pass.
- [x] 3.6 Implement `evaluate_unit_economics(inputs, policy)` as the single fail-closed public evaluator, with fixed reason ordering and immutable structured fallbacks; make malformed, unexpected-failure, repeated-evaluation, and purity contracts pass, then remove only helpers/imports made unused by this Change.

## 4. Route living documentation narrowly

- [x] 4.1 Update `references/gates.md` to keep high-level independent-gate guidance and route deterministic Unit Economics execution to `product_research/unit_economics.py`; remove only the stale “calculator is not implemented” statement and do not duplicate Decimal or threshold truth.
- [x] 4.2 Update `SKILL.md` routing and capability-gap text to state that deterministic Unit Economics is implemented while research adapters, normalization/acquisition, scoring, Risk automation, Red Team, persistence, and report generation remain unavailable.
- [x] 4.3 Review `tests/scenarios.md`, `references/gates.md`, and `SKILL.md` together to confirm Unit Economics rules are single-owned, no complete automated product-evaluation claim was introduced, and no Evidence Policy, assessment, scoring, or final-decision semantics were redefined.

## 5. Verify focused, regression, and scope gates

- [x] 5.1 Run `/usr/bin/python3 -m unittest discover -s tests -p 'test_unit_economics.py' -v`, repeat the ambient-context/reordered-ID/Unknown/reason-order cases, and confirm every delta-spec scenario traces to focused test evidence.
- [x] 5.2 Run the unchanged Phase 3 suites separately with `/usr/bin/python3 -m unittest discover -s tests -p 'test_evidence_data_model.py' -v`, `/usr/bin/python3 -m unittest discover -s tests -p 'test_evidence_policy.py' -v`, and `/usr/bin/python3 -m unittest discover -s tests -p 'test_evidence_assessment.py' -v`.
- [x] 5.3 Run `/usr/bin/python3 -m unittest discover -s tests -p 'test_*.py' -v` and every other repository-wide automated validation present at Apply time; leave the full suite green.
- [x] 5.4 Run `openspec validate add-unit-economics-engine --strict` and `openspec validate --all --strict`, then inspect the final diff for float money, ambient Decimal dependence, Unknown-to-zero behavior, hidden thresholds/defaults, FX, Dynamic Target heuristics, Evidence policy duplication, LLM inference, scoring/final decisions, third-party dependencies, generalized frameworks, upstream Phase 3 edits, package-export expansion, or unrelated changes.
- [x] 5.5 Obtain an independent acceptance review tracing proposal, design, every requirement/scenario, tasks, implementation, and fresh command output; resolve every in-scope finding and leave archive, commit, and push unperformed pending separate authorization.
